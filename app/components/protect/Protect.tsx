
import { ReactElement } from "react";

const Protect = ({children} : {children : ReactElement}) => {
    return (
        <div>
                        
        </div>
    );
};

export default Protect;
